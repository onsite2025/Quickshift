rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /documents/{nurseId}/{file=**} {
      allow read, write: if request.auth != null;
    }
    match /uploads/{file=**} {
      allow read, write: if request.auth != null;
    }
  }
}
